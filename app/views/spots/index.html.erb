<div id="index">
  <h2>Spot.IM Directory</h2>
  <%= link_to "Add New Spot", new_spot_path %>

  <% @spots.each do |spot| %>

      <li class='name'><%= link_to spot.company_name, spot_path(spot.id) %></li>

  <%end%>
</div>

